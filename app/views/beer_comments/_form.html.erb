<%= form_for([@beer, @beer_comment]) do |f| %>
  <% if @beer_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer_comment.errors.count, "error") %> prohibited this beer_comment from being saved:</h2>

      <ul>
      <% @beer_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
